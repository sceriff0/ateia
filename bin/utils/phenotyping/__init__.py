"""Panel-agnostic phenotyping: symbolic compiler + per-patient classification.

Import-only helpers (git-mode 100644). Keep this __init__ import-cheap: submodules
are imported explicitly by callers (e.g. `from phenotyping.panel_schema import ...`).
"""
