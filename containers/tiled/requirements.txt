# STARE tiled registration (registration_method='tiled') — CPU-only, JVM-free.
# Pinned to the versions the method was developed and tested against.
numpy==1.26.4
scipy==1.15.3
scikit-image==0.25.2
tifffile==2025.5.10
zarr==2.18.3          # lazy region reads for the streaming gigapixel stitch (tifffile aszarr)
numcodecs==0.13.1     # pinned: newer numcodecs drops blosc.cbuffer_sizes that zarr 2.18 imports
imagecodecs==2025.3.30 # zstd/LZW codecs for the stitched OME-TIFF

# torch==2.3.1 and kornia==0.7.3 are installed by the Dockerfile, NOT here: torch needs
# --index-url https://download.pytorch.org/whl/cpu (a requirements-file index directive
# would repoint EVERY package), and kornia must be installed AFTER torch or it drags the
# CUDA wheel from PyPI. tests/test_ci_stack_pinned.py knows about that split.
